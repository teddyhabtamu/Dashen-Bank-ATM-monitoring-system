FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir psycopg2-binary "pysnmp==6.2.6" requests

COPY common.py .
COPY sim_engine.py .
COPY snmp_agent.py .
COPY txn_engine.py .
COPY ej_engine.py .
COPY state_manager.py .

# All services override command:; this only sets a safe default.
CMD ["python3", "state_manager.py"]
